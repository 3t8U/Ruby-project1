require ('pry')
require ('rspec')
require ('Anagram')




describe('Anagram') do

  it("Takes a string, converts it to lowercase, removes all whitespace ")
  test = Anagram.new
      expect('HORSE', 'CAT').to(eq("horse", "cat"))
    end
